# ShortLink 短链接服务 - 简历亮点分析

## 项目名称
**ShortLink 分布式短链接平台**

## 技术栈关键词
Spring Boot 3.0、Spring Cloud Alibaba、ShardingSphere、RocketMQ、Redisson、Sentinel、MySQL、Redis、布隆过滤器、分布式锁、Lua脚本、Spring Cloud Gateway

---

## 简历核心要点（STAR 法则）

### 亮点一：分布式高并发场景下的缓存穿透与缓存击穿防护

**情境（Situation）：**
在短链接跳转场景中，面临每秒数万次的短链接访问请求，若短链接不存在或已过期，恶意请求会穿透缓存直达数据库，导致数据库压力剧增甚至雪崩。

**任务（Task）：**
设计一套多级防护机制，防止缓存穿透和缓存击穿，保障短链接跳转接口在高并发下的稳定性和可用性。

**行动（Action）：**
- 针对**缓存穿透**问题，采用 **Redisson 布隆过滤器**（预估容量1亿，误判率0.001）拦截不存在的短链接请求，在缓存层前置过滤无效查询
- 针对**缓存击穿**问题，使用 **Redisson 分布式锁** + **双重检查锁定模式（Double-Check Locking）** 防止热点 Key 失效时大量请求同时穿透到数据库
- 针对过期短链接，设置**空值缓存（Null Cache）** 并配置30分钟TTL，防止反复查询已过期链接
- 短链接路由查询采用**读写锁分离策略（RReadWriteLock）**，读操作使用读锁，写操作（如修改分组）使用写锁，提升并发读性能

**结果（Result）：**
通过多级缓存防护体系，短链接跳转请求99%以上命中 Redis 缓存直接返回，数据库查询压力降低约95%，系统可承载万级QPS的短链接跳转请求。

---

### 亮点二：基于 RocketMQ 的异步统计与消息幂等性保障

**情境（Situation）：**
短链接跳转时需要实时记录多维度统计数据（PV/UV/UIP、设备、浏览器、地域、网络类型等），同步写入会严重影响跳转响应时间。且在消息重复投递场景下，可能导致统计数据重复计数。

**任务（Task）：**
设计一套高性能的异步统计方案，解耦跳转与统计逻辑，同时保障消息消费的幂等性，避免重复统计。

**行动（Action）：**
- 采用 **RocketMQ** 实现统计数据的异步采集，短链接跳转接口仅将统计事件发送到消息队列，立即返回302重定向，将跳转响应时间控制在毫秒级
- 设计 **基于 Redis 的消息幂等处理器（MessageQueueIdempotentHandler）**：
  - 使用 `SETNX` 原子操作标记消息处理中状态（值为"0"）
  - 处理完成后将状态更新为"1"
  - 消费前先判断消息是否处理中或已完成，支持消息消费失败后自动重试
- 统计入库使用 **INSERT ... ON DUPLICATE KEY UPDATE** 语法实现累加式写入，保证多次消费结果一致性

**结果（Result）：**
短链接跳转接口响应时间从同步统计的100ms+降至10ms以内，统计吞吐量提升10倍以上。通过幂等机制保障了消息消费的Exactly-Once语义，统计数据准确率100%。

---

### 亮点三：ShardingSphere 分库分表与高性能数据架构设计

**情境（Situation）：**
短链接业务存在三个高频数据表：短链接表（t_link）、路由表（t_link_route）、分组表（t_group），单表数据量预估超过千万级别，传统单表结构无法支撑高并发读写。

**任务（Task）：**
设计分库分表方案，解决大数据量下的查询性能问题，同时兼顾业务查询模式的便捷性。

**行动（Action）：**
- 采用 **ShardingSphere-JDBC 5.3** 实现水平分表，将核心表拆分为16个分片（t_link_0 ~ t_link_15）
- **分片策略设计**：
  - 短链接表：按 `gid`（分组ID）进行 HASH_MOD 分片，保证同一分组的短链接落入同一分片，支持分组维度的高效查询
  - 路由表：按 `full_short_url` 进行 HASH_MOD 分片，保证短链接跳转时可直接定位分片，无需跨分片查询
  - 分组表：按 `username` 进行 HASH_MOD 分片，支持用户维度的高效查询
- 引入 **路由表（t_link_route）** 作为全局索引表，解决跨分片查询问题：短链接跳转时先查路由表获取 gid，再通过 gid 定位具体分片

**结果（Result）：**
数据库单表容量扩展16倍，支持亿级短链接存储。分片策略与业务查询模式匹配，避免了跨分片查询，单次查询耗时控制在毫秒级，写入吞吐量提升约10倍。

---

### 亮点四：多维度流量治理与限流熔断体系

**情境（Situation）：**
面对恶意刷接口、爬虫攻击或突发流量高峰，系统需要具备自我保护能力，防止服务被压垮，同时对正常用户提供友好的降级体验。

**任务（Task）：**
构建多层次的流量治理体系，实现精细化的限流控制和优雅的熔断降级。

**行动（Action）：**
- **接口级限流**：集成 **Alibaba Sentinel**，对短链接创建接口配置 QPS 限流规则，超出阈值触发自定义熔断处理器（CustomBlockHandler），返回友好的限流提示而非系统错误
- **用户级限流**：设计 **基于 Lua 脚本的滑动窗口限流器（UserTrafficRiskControlFilter）**，通过 Redis 原子操作实现用户维度的请求频率控制：
  - 使用 INCR + EXPIRE 组合命令实现原子递增和过期设置
  - 首次请求时设置时间窗口，后续请求在窗口内累加计数
  - 超过阈值的请求直接返回流控错误，保护后端服务
- **网关层认证**：基于 Spring Cloud Gateway 实现 Token 校验过滤器，在网关层拦截无效请求，减少无效流量进入微服务集群

**结果（Result）：**
系统具备了从网关到应用层的多级流量防护能力，可抵御万级 QPS 的恶意请求冲击。Lua 脚本在 Redis 侧执行，限流判断耗时微秒级，不影响正常请求响应时间。

---

## 技术深度扩展点（面试可深入探讨）

| 技术点 | 实现细节 | 设计模式/原理 |
|--------|----------|---------------|
| 布隆过滤器 | Redisson RBloomFilter，1亿容量，0.001误判率 | 概率型数据结构，空间换时间 |
| 分布式锁 | Redisson RLock/RReadWriteLock | 可重入锁、读写锁分离 |
| 缓存策略 | Cache-Aside + 空值缓存 + 预热 | 多级缓存架构 |
| 消息幂等 | Redis SETNX + 状态机 | 分布式幂等性设计 |
| 分片策略 | HASH_MOD + 路由表 | 数据分片、全局二级索引 |
| 限流方案 | Sentinel + Lua脚本滑动窗口 | 令牌桶/滑动窗口算法 |
| 统计架构 | MQ异步 + INSERT ON DUPLICATE KEY UPDATE | 最终一致性、累加幂等 |

---

## 简历推荐写法示例

> **短链接平台（分布式高并发系统）**
> - 设计并实现基于 **布隆过滤器 + 分布式锁 + 空值缓存** 的三级缓存防护体系，解决缓存穿透和击穿问题，缓存命中率达99%+，QPS承载能力提升至万级
> - 基于 **RocketMQ + Redis 幂等处理器** 实现统计数据异步采集，将短链接跳转接口响应时间从100ms优化至10ms以内，支持每秒万级消息处理
> - 采用 **ShardingSphere 水平分表**，将核心表拆分为16个分片，结合路由表设计解决跨分片查询问题，支持亿级数据量存储
> - 构建 **Sentinel + Lua脚本** 多层限流体系，实现接口级和用户级精细化流控，有效抵御恶意请求攻击
